// JavaScript Document

$(document).ready(function(){
            $(".jqzoom").jqueryzoom({
                xzoom:500,  // 放大图的宽
                yzoom:500,  // 放大图的高
                offset:30,   // 放大图距离原图的位置
                position:'right'  // 放大图在原图的右边(默认为right)
            });
        });